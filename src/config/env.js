const sat = {
  baseUrl: "http://pstest.dfth.com/"
};

const uat = {
  baseUrl: "http://pstest.dfth.com/"
};

const pre = {
  baseUrl: "http://pstest.dfth.com/"
};

const prd = {
  baseUrl: "http://pandatest.dfth.com/"
};

export const env = { sat, uat, pre, prd };
export default env;
